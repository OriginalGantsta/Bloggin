import { Url } from "url";

export class Blog {
  title: string;
  author: string;
  image?: string;
  imageDescription?: string;
  date: number;
  blurb: string;
  bodyText: string;
  uid?: string;
}
